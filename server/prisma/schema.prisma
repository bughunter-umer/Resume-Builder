model Resume {
  id          Int      @id @default(autoincrement())
  name        String
  email       String
  phone       String?
  address     String?   //for multiple address
  education   String?
  experience  Json?     // for multiple jobs
  projects    Json?     // for multiple projects
  skills      Json?     // array of skills
}

